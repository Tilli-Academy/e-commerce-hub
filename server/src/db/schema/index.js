const { users } = require('./users');
const { products, productImages } = require('./products');
const { reviews } = require('./reviews');
const { carts, cartItems } = require('./carts');
const { orders, orderItems } = require('./orders');

module.exports = {
  users,
  products,
  productImages,
  reviews,
  carts,
  cartItems,
  orders,
  orderItems,
};
